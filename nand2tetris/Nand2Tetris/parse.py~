#!/usr/bin/python
import sys, getopt

def main(argv):
    infile = "aaa.hack"
    outfile = "bbb.hack"
    f = open(infile,"r")
    g = open(outfile, "w+")
    code = f.read()

    parsed = bin(int(code, 16))[2:].zfill(16)
    print(parsed)
    g.write(parsed)
    f.close()
    g.close()

